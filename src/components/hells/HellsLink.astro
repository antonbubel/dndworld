---
import type { CollectionEntry } from "astro:content";

interface Props {
  hellsEntry: CollectionEntry<"hells">;
}

const { hellsEntry} = Astro.props;
---

{hellsEntry && <a href={`/hells/${hellsEntry.slug}`}>{hellsEntry.data.title}</a>}
